**Lambda function.
-I think it quite a good part of coding, instead you use a class to implements any interfaces, may be it's timeless. With lambda you can take the functional interface immediately and don't have to use any class
